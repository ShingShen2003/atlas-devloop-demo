# Compliance test scratch

Deliberate change on a branch with no story id, to verify the server-side
`atlas-compliance` check blocks a PR/merge that references no story/task.
This file + branch are deleted after the check is observed.
